<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/style.css">
  <title>Edit <%= food.name %></title>
</head>

<body>
  <%- include("../partials/_navbar.ejs") %>
  <h1>Edit <%= food.name %></h1>

  <form action="/users/<%= user._id %>/foods/<%= food._id%>?_method=PUT" method="POST">
    <label for="name">Name: </label>
    <input value="<%= food.name %>" type="text" name="name" id="name" required>

    <label for="quantity">Quantity: </label>
    <input value="<%= food.quantity %>" type="number" name="quantity" id="quantity">

    <label for="unit">Unit: </label>
    <input value="<%= food.unit %>" type="text" name="unit" id="unit">

    <button type="submit">Edit food</button>

  </form>

</body>

</html>